---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Prometheus"
    path: "prometheus.yaml"
  - name: "NodeExporter"
    path: "node_exporter.yaml"
  - name: "Pushgateway"
    path: "pushgateway.yaml"
